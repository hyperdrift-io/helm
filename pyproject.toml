[project]
name = "helm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "google-adk>=2.8.0",
    "google-cloud-firestore>=2.29.0",
    "httpx>=0.28.1",
    "mcp>=1.10,<2",
    "uvicorn>=0.52.4",
]
